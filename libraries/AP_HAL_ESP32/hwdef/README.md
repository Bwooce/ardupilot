# ESP32 Board Naming Standard

This document outlines the naming standard for ESP32 boards within the ArduPilot ecosystem. Consistent naming is crucial for clarity and ease of use.

## Standard

The naming convention is `esp32<board_name>`. For example, a board named `lilygo_tconnect` becomes `esp32lilygo_tconnect`.
